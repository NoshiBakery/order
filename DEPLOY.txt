DEPLOY — exact order
====================

A) CLOUDFLARE WORKER (DO THIS FIRST)
Replace the current internal Worker code with:
  internal-worker.js
Then Deploy.

Keep the same bindings/secrets/variable:
- DB -> noshi-internal
- ORDERS_DB -> noshi-orders
- OWNER_PASSWORD secret
- MOTHER_PASSWORD secret
- INTERNAL_ORIGIN = https://noshibakery.github.io

B) OPTIONAL SAFE D1 INDEXES
Run on noshi-internal:
  cloudflare/noshi-internal-performance.sql
Run on noshi-orders:
  cloudflare/noshi-orders-performance.sql
These commands only CREATE INDEX IF NOT EXISTS. They do not delete data.

C) GITHUB
Upload the changed/new frontend files from this ZIP, preserving folders.
Important new folders/files:
- images/Noshi.webp
- images/thumbs/*.webp

D) TEST
1. Login on owner phone.
2. Open orders / clients / dashboard and compare load time.
3. Create a temporary order with quantity > 1, open split delivery, add second schedule, drag product cards only, save.
4. Verify both delivery slots appear under the correct dates/mandoubs.
5. Open mother phone and verify the same live data.

No migration.html and no JSON re-import are needed for this patch.
